//Generated by TurboLink CodeGenerator, do not edit!
#pragma once
#include "CoreMinimal.h"
#include "Kismet/BlueprintAsyncActionBase.h"
#include "TurboLinkGrpcClient.h"
#include "BusMessage.h"
#include "BusNode.generated.h"

class UBusService;
class UBusServiceClient;

UCLASS(ClassGroup = TurboLink)
class TURBOLINKGRPC_API UCallBusServiceResetReaderBus : public UBlueprintAsyncActionBase
{
	GENERATED_BODY()

public:
	DECLARE_DYNAMIC_MULTICAST_DELEGATE_TwoParams(FBusServiceResetReaderBusDelegate, const FGrpcResult&, GrpcResult, const FGrpcSroResetBusResponse&, Response);

	UFUNCTION(BlueprintCallable, meta = (BlueprintInternalUseOnly = "true", WorldContext = "WorldContextObject", DisplayName = "Call BusService ResetReaderBus"), Category = "TurboLink|BusService")
	static UCallBusServiceResetReaderBus* ResetReaderBus(UObject* WorldContextObject, const FGrpcSroBusTarget& Request, TMap<FString, FString>& MetaData);

	UPROPERTY(BlueprintAssignable)
	FBusServiceResetReaderBusDelegate OnResetReaderBusResponse;

	UPROPERTY(BlueprintAssignable)
	FBusServiceResetReaderBusDelegate OnFail;

private:
	virtual void Activate() override;

	UPROPERTY()
	TObjectPtr<UBusService> BusService;
	
	UPROPERTY()
	TObjectPtr<UBusServiceClient> BusServiceClient;
	
	FGrpcContextHandle Context;
	FGrpcSroBusTarget Request;
	EGrpcServiceState ServiceState;
	TMap<FString, FString> MetaData;

	UFUNCTION()
	void OnServiceStateChanged(EGrpcServiceState NewState);
	
	UFUNCTION()
	void OnContextStateChange(FGrpcContextHandle Handle, EGrpcContextState State);

	UFUNCTION()
	void OnResponse(FGrpcContextHandle Handle, const FGrpcResult& GrpcResult, const FGrpcSroResetBusResponse& Response);

	void Shutdown();
};

UCLASS(ClassGroup = TurboLink)
class TURBOLINKGRPC_API UCallBusServiceResetWriterBus : public UBlueprintAsyncActionBase
{
	GENERATED_BODY()

public:
	DECLARE_DYNAMIC_MULTICAST_DELEGATE_TwoParams(FBusServiceResetWriterBusDelegate, const FGrpcResult&, GrpcResult, const FGrpcSroResetBusResponse&, Response);

	UFUNCTION(BlueprintCallable, meta = (BlueprintInternalUseOnly = "true", WorldContext = "WorldContextObject", DisplayName = "Call BusService ResetWriterBus"), Category = "TurboLink|BusService")
	static UCallBusServiceResetWriterBus* ResetWriterBus(UObject* WorldContextObject, const FGrpcSroBusTarget& Request, TMap<FString, FString>& MetaData);

	UPROPERTY(BlueprintAssignable)
	FBusServiceResetWriterBusDelegate OnResetWriterBusResponse;

	UPROPERTY(BlueprintAssignable)
	FBusServiceResetWriterBusDelegate OnFail;

private:
	virtual void Activate() override;

	UPROPERTY()
	TObjectPtr<UBusService> BusService;
	
	UPROPERTY()
	TObjectPtr<UBusServiceClient> BusServiceClient;
	
	FGrpcContextHandle Context;
	FGrpcSroBusTarget Request;
	EGrpcServiceState ServiceState;
	TMap<FString, FString> MetaData;

	UFUNCTION()
	void OnServiceStateChanged(EGrpcServiceState NewState);
	
	UFUNCTION()
	void OnContextStateChange(FGrpcContextHandle Handle, EGrpcContextState State);

	UFUNCTION()
	void OnResponse(FGrpcContextHandle Handle, const FGrpcResult& GrpcResult, const FGrpcSroResetBusResponse& Response);

	void Shutdown();
};
